package config

const (
	EnvConfigHome = "CONFIG_HOME"
	EnvLogHome    = "LOG_HOME"
	EnvRunPort    = "RUN_PORT"
)

const (
	LogLocal = "log.local"
	LogHome  = "log.home"
	LogFile  = "log.file"
	LogColor = "log.color"
)

const (
	CronEnable = "cron.enable"
)

const (
	StoreFeedType = "store.feed.type"
)
